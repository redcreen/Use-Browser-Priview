{
  "name": "use-browser-priview",
  "private": true,
  "version": "0.0.1",
  "description": "Standalone browser preview project for Finder and editor context menus.",
  "scripts": {
    "install:all": "bash install.sh --all",
    "install:vscode": "bash install.sh --vscode",
    "install:finder": "bash install.sh --finder",
    "check:vscode-adapter": "node --check adapters/vscode/extension.js && node --check adapters/vscode/open-finder-preview.js && bash -n adapters/vscode/open-finder-preview.sh && bash -n adapters/vscode/install-macos-finder-quick-action.sh",
    "check:installers": "bash -n install.sh && bash -n install.command && bash -n install-vscode.command && bash -n install-finder.command",
    "test:finder-shared-session-reuse": "node tests/validate-finder-shared-session-reuse.mjs",
    "test:shared-session-store": "node tests/validate-shared-session-store.mjs",
    "test:vscode-adapter-contract": "node tests/validate-vscode-adapter.mjs",
    "test:install-flows": "node tests/validate-install-flows.mjs",
    "test": "npm run check:vscode-adapter && npm run check:installers && npm run test:vscode-adapter-contract && npm run test:shared-session-store && npm run test:finder-shared-session-reuse && npm run test:install-flows"
  }
}
